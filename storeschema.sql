CREATE TABLE `toughguy`.`storedb` (
  `store` VARCHAR(60) NOT NULL,
  `item` VARCHAR(45) NOT NULL,
  `count` VARCHAR(45) NOT NULL,
  `latitude` VARCHAR(45) NOT NULL,
  `longitude` VARCHAR(45) NOT NULL,
  `ID` VARCHAR(100) NULL);